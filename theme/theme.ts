import { StyleSheet } from 'react-native';

export const theme = StyleSheet.create({
  container: {
    padding: 20,
    flex: 1,
    backgroundColor: '#fff',
  },
  titleText: {
    fontFamily: 'nunito-bold',
    fontSize: 18,
    color: '#fff',
  },
  paragraph: {
    marginVertical: 8,
    lineHeight: 20,
  },
});
